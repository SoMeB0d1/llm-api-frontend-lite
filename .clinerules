# LLM API Frontend Lite - Cline Rules

## 项目角色
你专门负责构建 frontend/ 目录下的前端静态页面，并将其与 Node.js 后端（代理 Go LLM API）对接。

## 约束规则
- **禁止修改 backend/ 目录下的 Go 代码**，除非用户明确要求。
- **仅修改 frontend/ 下的文件**（及相关配置文件），除非用户另有指示。
- **保持 HTML/CSS/JS 简洁可读**，避免引入重型框架，除非用户要求。
- **仅桌面端布局**，确保适配任意浏览器宽度。
- **禁止页面级滚动**，只有侧边栏历史列表和聊天历史区域可滚动。

## 布局规范
1. **响应式布局**：侧边栏网格 + 主聊天区（auto/1fr），<800px 时使用汉堡菜单，800-1200px 侧边栏宽度 240px，≥1200px 侧边栏宽度 280px。
2. **侧边栏结构**：Logo、用户 ID、设置按钮、新建聊天按钮、历史主题列表。
3. **聊天视图**：用户消息右对齐黄色气泡，模型消息全宽渲染（支持 Markdown/LaTeX），输入框 + 发送按钮。

## API 模式
支持四种 API 调用模式：
1. **提示/回答**：prompt/answer 模式
2. **历史记录**：带本地 JSON 缓存的历史记录获取
3. **Token 验证/刷新**：token 校验与刷新
4. **用户名/密码登录**：登录认证

## 输出格式
- 简要变更摘要
- 涉及的文件列表
- API 对接说明及待解决问题
- 建议的后续步骤（测试或手动检查）
- **使用中文输出所有内容**